# Benchmarking Workflow Diagram

## Visual Workflow

```
┌─────────────────────────────────────────────────────────────────────┐
│                      AMD vs NVIDIA GPU COMPARISON                    │
└─────────────────────────────────────────────────────────────────────┘

STEP 1: Run on NVIDIA GPU
┌─────────────────────────────────┐
│  NVIDIA System                  │
│  ┌───────────────────────────┐  │
│  │ ./run_benchmark.sh llama  │  │
│  │         OR                │  │
│  │ python3 pretrain_llama.py │  │
│  └───────────────────────────┘  │
│              ↓                  │
│  ┌───────────────────────────┐  │
│  │  Training with NeMo       │  │
│  │  - Auto-detects CUDA      │  │
│  │  - Collects metrics       │  │
│  │  - Times each step        │  │
│  │  - Monitors memory        │  │
│  └───────────────────────────┘  │
│              ↓                  │
│  ┌───────────────────────────┐  │
│  │  benchmark_results/       │  │
│  │  benchmark_cuda_*.json    │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘

                 ⬇️

STEP 2: Run on AMD GPU
┌─────────────────────────────────┐
│  AMD System                     │
│  ┌───────────────────────────┐  │
│  │ ./run_benchmark.sh llama  │  │
│  │         OR                │  │
│  │ python3 pretrain_llama.py │  │
│  └───────────────────────────┘  │
│              ↓                  │
│  ┌───────────────────────────┐  │
│  │  Training with NeMo       │  │
│  │  - Auto-detects ROCm      │  │
│  │  - Collects metrics       │  │
│  │  - Times each step        │  │
│  │  - Monitors memory        │  │
│  └───────────────────────────┘  │
│              ↓                  │
│  ┌───────────────────────────┐  │
│  │  benchmark_results/       │  │
│  │  benchmark_rocm_*.json    │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘

                 ⬇️

STEP 3: Compare Results
┌─────────────────────────────────┐
│  Any System                     │
│  ┌───────────────────────────┐  │
│  │ python3 compare_results.py│  │
│  └───────────────────────────┘  │
│              ↓                  │
│  ┌───────────────────────────┐  │
│  │  Analysis & Visualization │  │
│  │  - Load both JSON files   │  │
│  │  - Calculate speedup      │  │
│  │  - Generate charts        │  │
│  │  - Create report          │  │
│  └───────────────────────────┘  │
│              ↓                  │
│  ┌───────────────────────────┐  │
│  │  Output Files:            │  │
│  │  ✓ comparison_plot.png    │  │
│  │  ✓ comparison_report.md   │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘

                 ⬇️

RESULT
┌─────────────────────────────────────────────────────────────┐
│  🏆 NVIDIA is 1.26x FASTER                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  NVIDIA: 1.245s/step  |  0.803 steps/s  |  45.89GB        │
│  AMD:    1.567s/step  |  0.638 steps/s  |  48.23GB        │
└─────────────────────────────────────────────────────────────┘
```

## Component Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                    BENCHMARKING SYSTEM                         │
└────────────────────────────────────────────────────────────────┘

Core Components:
┌──────────────────────────┐
│  benchmark_utils.py      │  ← Core benchmarking framework
│  ┌────────────────────┐  │
│  │ BenchmarkCallback  │  │  - Platform detection (CUDA/ROCm)
│  │                    │  │  - Metric collection
│  │ - on_train_start() │  │  - Timing measurements
│  │ - on_batch_start() │  │  - Memory monitoring
│  │ - on_batch_end()   │  │  - Result saving
│  │ - on_train_end()   │  │  - Summary generation
│  └────────────────────┘  │
└──────────────────────────┘

Training Scripts (Updated):
┌────────────────────┐  ┌────────────────────┐  ┌────────────────────┐
│ pretrain_llama.py  │  │ pretrain_qwen.py   │  │ pretrain_mistral.py│
│                    │  │                    │  │                    │
│ + BenchmarkCallback│  │ + BenchmarkCallback│  │ + BenchmarkCallback│
└────────────────────┘  └────────────────────┘  └────────────────────┘

Analysis Tools:
┌─────────────────────────┐  ┌──────────────────────────┐
│  compare_results.py     │  │  analyze_existing_logs.py│
│                         │  │                          │
│  - Load JSON results    │  │  - Check AMD logs        │
│  - Calculate metrics    │  │  - Check NVIDIA logs     │
│  - Generate plots       │  │  - Show summary          │
│  - Create report        │  │  - Compare with new      │
└─────────────────────────┘  └──────────────────────────┘

Automation:
┌────────────────────┐
│  run_benchmark.sh  │
│                    │
│  - Detect platform │
│  - Select model    │
│  - Run N times     │
│  - Show results    │
└────────────────────┘
```

## Data Flow

```
Training Script (pretrain_*.py)
    │
    ├─> NeMo Recipe Configuration
    │   └─> Model, Data, Trainer settings
    │
    ├─> BenchmarkCallback (from benchmark_utils.py)
    │   │
    │   ├─> on_train_start()
    │   │   └─> Detect platform (CUDA/ROCm)
    │   │   └─> Get GPU info
    │   │
    │   ├─> on_train_batch_start()
    │   │   └─> Record start time
    │   │   └─> Sync CUDA/ROCm
    │   │
    │   ├─> on_train_batch_end()
    │   │   └─> Calculate step time
    │   │   └─> Record memory usage
    │   │   └─> Log metrics
    │   │
    │   └─> on_train_end()
    │       └─> Calculate statistics
    │       └─> Save to JSON
    │       └─> Print summary
    │
    └─> Output: benchmark_results/benchmark_{platform}_{timestamp}.json


compare_results.py
    │
    ├─> Load benchmark_cuda_*.json
    ├─> Load benchmark_rocm_*.json
    │
    ├─> Calculate:
    │   ├─> Speedup factor
    │   ├─> Throughput ratio
    │   ├─> Memory comparison
    │   └─> Statistical analysis
    │
    ├─> Generate:
    │   ├─> comparison_plot.png (4 charts)
    │   └─> comparison_report.md (detailed analysis)
    │
    └─> Print: Summary to console
```

## File Interactions

```
Input Files:
    pretrain_llama.py     ──┐
    pretrain_qwen.py      ──┼──> Uses ──> benchmark_utils.py
    pretrain_mistral.py   ──┘

Generated Files:
    benchmark_results/
    ├── benchmark_cuda_TIMESTAMP.json    ←─ From NVIDIA run
    ├── benchmark_rocm_TIMESTAMP.json    ←─ From AMD run
    │
    └── (After comparison)
        ├── comparison_plot.png          ←─ From compare_results.py
        └── comparison_report.md         ←─ From compare_results.py

Documentation:
    QUICK_START.md         ─── Quick reference
    BENCHMARK_README.md    ─── Detailed guide
    WORKFLOW.md            ─── This file
    BENCHMARKING_GUIDE.md  ─── Complete overview
```

## Quick Commands Reference

```bash
# 1. SETUP
pip install matplotlib numpy

# 2. RUN ON NVIDIA
./run_benchmark.sh llama              # Single run
./run_benchmark.sh llama 5            # 5 runs

# 3. RUN ON AMD
./run_benchmark.sh llama              # Single run
./run_benchmark.sh llama 5            # 5 runs

# 4. COMPARE
python3 compare_results.py

# 5. ANALYZE OLD LOGS
python3 analyze_existing_logs.py

# 6. OTHER MODELS
./run_benchmark.sh qwen               # Qwen 2.5 7B
./run_benchmark.sh mistral            # Mistral 7B
```

## Metrics Collection Timeline

```
Training Step Timeline:
┌──────────────────────────────────────────────────────┐
│                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │   Step 0    │  │   Step 1    │  │   Step 2    │ │
│  │  (Warmup)   │  │  (Counted)  │  │  (Counted)  │ │
│  └─────────────┘  └─────────────┘  └─────────────┘ │
│        │                 │                │         │
│        │                 │                │         │
│        ▼                 ▼                ▼         │
│   [SKIPPED]         [MEASURED]       [MEASURED]    │
│                          │                │         │
│                          ├─ Time         │         │
│                          ├─ Memory       │         │
│                          └─ ...          │         │
│                                          │         │
│                     Final Statistics ◄──┴─────────┤
│                     (Avg, Min, Max, Variance)     │
└──────────────────────────────────────────────────────┘

Measurement Points per Step:
on_train_batch_start()
    ↓
    Start Timer
    Sync GPU
    ↓
  [Training happens here - forward, backward, optimizer]
    ↓
on_train_batch_end()
    ↓
    Sync GPU
    Stop Timer
    Record Memory
    Calculate Metrics
    Log Results (every 10 steps)
```

## Comparison Output Structure

```
comparison_plot.png:
┌────────────────────────────────────────┐
│  ┌─────────────┐  ┌─────────────┐     │
│  │ Avg Step    │  │ Throughput  │     │
│  │    Time     │  │             │     │
│  │   (bars)    │  │   (bars)    │     │
│  └─────────────┘  └─────────────┘     │
│  ┌─────────────┐  ┌─────────────┐     │
│  │   Memory    │  │  Step Time  │     │
│  │   Usage     │  │ Distribution│     │
│  │   (bars)    │  │  (line plot)│     │
│  └─────────────┘  └─────────────┘     │
└────────────────────────────────────────┘

comparison_report.md:
┌────────────────────────────────────────┐
│ # AMD vs NVIDIA GPU Comparison         │
│                                        │
│ ## Executive Summary                   │
│ Winner: NVIDIA is 1.26x faster         │
│                                        │
│ ## Hardware Configuration              │
│ [Device specs for both platforms]      │
│                                        │
│ ## Performance Metrics                 │
│ [Detailed tables and analysis]         │
│                                        │
│ ## Memory Usage                        │
│ [Memory comparison tables]             │
│                                        │
│ ## Detailed Analysis                   │
│ [Statistical breakdown]                │
└────────────────────────────────────────┘
```

---

**Need help?** Check:
- `QUICK_START.md` for TL;DR
- `BENCHMARK_README.md` for detailed docs
- `BENCHMARKING_GUIDE.md` for complete overview

