# AMD Container Requirements
# PyTorch ROCm support (installed from base image)

# Core training frameworks
transformers>=4.36.0
accelerate>=0.30.0
datasets>=2.14.0
tokenizers>=0.15.0

# Distributed training frameworks
# DeepSpeed with ROCm support (check compatibility with your ROCm version)
deepspeed>=0.12.0

# NeMo Framework (AMD/ROCm compatible version)
# Install nemo_toolkit (without [all] extras to avoid conflicts)
# Note: NeMo works on ROCm via PyTorch's unified API

# AMD Primus framework (ROCm-optimized, installed from base image)
# primus @ git+https://github.com/amd/primus.git

# Memory optimization
# bitsandbytes for ROCm (if available)
# bitsandbytes-rocm

# Performance optimizations
# Flash Attention for ROCm
# flash-attn (ROCm-compatible version)
ninja
packaging<26.0  # Pin for compatibility with mlflow, pyvers

# Utilities
scipy
numpy>=1.24.0
sentencepiece
protobuf
pydantic>=2.0.0
pyyaml
cryptography>=43.0.0,<46  # Pin for mlflow compatibility

# Monitoring and logging
tensorboard
wandb

# Optional: Model conversion and optimization
onnx
einops

# Development tools
pytest
black
isort
